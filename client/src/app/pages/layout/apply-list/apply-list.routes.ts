import { ApplyListComponent } from './apply-list.component';
import { Routes } from '@angular/router';

export const APPLYLIST_ROUTERS: Routes = [
  {
    path: '',
    component: ApplyListComponent,
  },
];