import { ApplicationListComponent } from './application-list.component';
import { Routes } from '@angular/router';

export const APPLICATIONLIST_ROUTERS: Routes = [
  {
    path: '',
    component: ApplicationListComponent,
  },
];